{
  "name": "pwd-gen",
  "private": true,
  "version": "0.0.0",
  "description": "Password generator",
  "license": "MIT",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-copy": "^0.5.0",
    "bower": "^1.3.9",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "grunt && http-server -a 0.0.0.0 -p 3000"
  }
}
